{
  "name": "sized-service",
  "version": "0.0.1",
  "description": "Web service with specific URIs to return data of specific size",
  "main": "sized.js",
  "scripts": {
    "test": "standard && echo \"Error: no test specified\" && exit 1"
  },
    "author": "",
    "license": "Apache",
    "devDependencies": {
        "standard": "^3.0.0"
    },    
    "dependencies": {
        "express": "^4.13.3"
    }
}
